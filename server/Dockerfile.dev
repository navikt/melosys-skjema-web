FROM node:22-slim

WORKDIR /app

# Copy package files
COPY package*.json ./

# Mount npmrc as secret and install
RUN --mount=type=secret,id=npmrc,target=/root/.npmrc npm ci

# Copy source
COPY . .

# Build TypeScript
RUN npm run build

EXPOSE 8081
CMD ["node", "dist/index.js"]
